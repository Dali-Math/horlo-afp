// src/lib/planning-manager.ts
export function parsePlanning(pdfData: string) {
  console.warn("⚠️ planning-manager: fonction temporaire, à compléter.");
  return [];
}

export function updatePlanning(existing: any, newData: any) {
  console.warn("⚠️ planning-manager: fonction update temporaire, à compléter.");
  return newData;
}
